"""Core utilities for the Factora backend.

Sub-packages:
    security  — hashing primitives (Argon2id, SHA-256) and JWT helpers.
    demo      — demo-fixture decorator for mocking external API responses.
    config    — re-export shim for ``app.config.settings``.
    exceptions — domain exception hierarchy.
"""
